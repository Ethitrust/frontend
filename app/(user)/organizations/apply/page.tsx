import { OrganizationsApplyRoute } from '@/components/organizations/organizations-user-pages'

export const metadata = {
  title: 'Apply — Organization — Ethi-Trust',
  description: 'Apply for an organization workspace.',
}

export default function OrganizationApplyPage() {
  return <OrganizationsApplyRoute />
}
