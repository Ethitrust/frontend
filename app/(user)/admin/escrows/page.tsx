import { AdminEscrowsRoutedPage } from '@/components/admin/admin-platform-routed-pages'

export const metadata = {
  title: 'Escrows — Ethi-Trust Admin',
  description: 'Operator view of escrow records across the platform.',
}

export default function AdminEscrowsPage() {
  return <AdminEscrowsRoutedPage />
}
