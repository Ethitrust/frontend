import { AdminSupportCasesRoutedPage } from '@/components/admin/admin-configuration-routed-pages'

export const metadata = {
  title: 'Support cases — Ethi-Trust Admin',
  description: 'Operator queue for inbound support tickets.',
}

export default function AdminSupportCasesPage() {
  return <AdminSupportCasesRoutedPage />
}
